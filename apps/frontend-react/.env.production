# Hosted Amplify / production builds talk to the Windows EC2 backend (HTTP for now).
# Browsers block mixed content from https:// Amplify → http:// API until TLS is added on EC2.
VITE_API_BASE_URL=http://ec2-13-232-110-145.ap-south-1.compute.amazonaws.com:8000
