# Default for `npm run dev` — local UI talks to local backend.
VITE_API_BASE_URL=http://localhost:8000

# Restore previous production API from a local Vite session:
# VITE_USE_PRODUCTION_API=true
#
# Production builds still use `.env.production` (https://api.signalbridge.in).
