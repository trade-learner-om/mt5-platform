# Conditional Orders section under Positions → Orders

## Summary

Positions → Orders now has a dedicated **Conditional Orders** panel (trigger, entry, SL, cancel). Live websocket order payloads include `manual_context` so trigger price is visible while status is `WAITING_TRIGGER`.
