# Format Master Break Max DD Period as Date-Time

## Summary

`max_dd_period.from` / `to` are now formatted as `YYYY-MM-DD HH:MM:SS` instead of raw unix timestamps. Trade exit times in the simulator use ISO strings, and the summary UI formats the period as Date-Time.
